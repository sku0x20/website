{
  "name": "www.sku20.dev",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run build:slides",
    "build:slides": "node scripts/build-slides.mjs",
    "build:all": "npm run build",
    "build:pdf": "node scripts/generate-pdf.mjs",
    "slides:dev": "slidev",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.2.0"
  },
  "devDependencies": {
    "@slidev/cli": "^52.19.1",
    "@slidev/theme-default": "^0.25.0"
  }
}
